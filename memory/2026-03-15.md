# 2026-03-15

## Conversation Save: software-development-skill
- Context:
  - user first asked whether folder-level `index.md` or similar files were a good way to conserve context while navigating code
  - the conversation then turned into implementing a reusable coding-project workflow around folder `README.md` files and stronger verification rules
- User goal:
  - create a reusable skill for coding projects
  - rename the skill to `software-development`
  - require README upkeep for documented subsystem folders
  - require a successful final build before calling work done
  - require at least one direct verification of final delivered behavior or output, not just indirect unit checks
- What was inspected or executed:
  - read project persistence/runtime context from `AGENTS.md`, `MEMORY.md`, and `memory/2026-03-14.md`
  - read skill/persistence guidance from:
    - `~/.codex/skills/.system/skill-creator/SKILL.md`
    - `~/.codex/skills/save-conversation/SKILL.md`
    - `~/.codex/skills/save-knowledge/SKILL.md`
    - `~/.codex/skills/commit/SKILL.md`
    - `~/.codex/notes/INDEX.md`
    - `~/.codex/notes/skill_instruction_design_patterns.md`
  - initialized and validated the new global skill with:
    - `python3 ~/.codex/skills/.system/skill-creator/scripts/init_skill.py software-development ...`
    - `python3 ~/.codex/skills/.system/skill-creator/scripts/generate_openai_yaml.py ...`
    - `python3 ~/.codex/skills/.system/skill-creator/scripts/quick_validate.py ...`
  - resolved save targets with:
    - `python3 ~/.codex/skills/save-knowledge/scripts/resolve_targets.py --intent conversation --project-root ... --topic software-development-skill --ensure-dirs --json`
- Key files created or updated:
  - global:
    - `~/.codex/skills/software-development/SKILL.md`
    - `~/.codex/skills/software-development/agents/openai.yaml`
    - `~/.codex/notes/software_development_verification_contract.md`
    - `~/.codex/notes/INDEX.md`
  - project:
    - `AGENTS.md`
    - `README.md`
    - `Scrollapp/README.md`
    - `ScrollappTests/README.md`
    - `MEMORY.md`
    - `memory/2026-03-15.md`
- Key findings and decisions:
  - folder-level `README.md` files are useful as low-token code maps when limited to meaningful subsystem boundaries rather than every folder
  - the initial narrow `module-readme-maintainer` skill was replaced with a broader `software-development` skill
  - the project now explicitly requires:
    - reading the nearest README chain first
    - updating relevant folder READMEs with code changes
    - a successful final app build before sign-off
    - at least one direct verification of real delivered behavior
  - for Scrollapp specifically, autoscroll work should verify real scrolling through the built app's actual event path using automation, instrumentation, or a documented manual fixture check
- Open issues or next steps:
  - the repo still has unrelated in-progress autoscroll/code changes outside this save flow
  - the new verification contract is documented, but the behavior-critical runtime path still needs future work to satisfy it reliably on live autoscroll changes
