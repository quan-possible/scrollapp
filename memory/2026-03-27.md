# 2026-03-27

## Conversation Save: runtime-simplify-and-parity-guardrails
- Context:
  - the user asked to simplify the app aggressively while preserving identical behavior, then reported a live scroll-speed regression after the simplification landed
- User goals:
  - simplify the app to the bare minimum without changing behavior
  - restore the previous live autoscroll feel when the simplification changed it
  - harden the simplification skill so future cleanup passes must prove parity instead of assuming it
  - commit, push, and save the resulting state
- What was inspected or executed:
  - mapped the runtime around `Scrollapp/AutoscrollCore.swift` and `Scrollapp/ScrollappApp.swift`
  - simplified the runtime, tests, Xcode project, and repo docs
  - verified the simplified app through:
    - `./scripts/verify_autoscroll_delivery.sh`
    - `./scripts/verify_launch_smoke.sh`
    - `./scripts/verify_autoscroll_external_no_cursor.sh`
  - debugged the speed regression and restored the earlier rate-based `1.0 / 100.0` emission model
  - raised the top-end speed ceiling by updating the physics curve in `Scrollapp/AutoscrollCore.swift`
  - updated the live global `simplify` skill and durable simplify note under `~/.codex` so future simplification work requires:
    - a pre-edit baseline
    - a rollback-safe checkpoint
    - an explicit old-vs-new parity gate before sign-off
  - committed and pushed the repo batch as `68f6f91 simplify runtime and restore scroll behavior`
- Key files created or updated:
  - app/runtime:
    - `Scrollapp/AutoscrollCore.swift`
    - `Scrollapp/ScrollappApp.swift`
    - `ScrollappTests/AutoscrollCoreTests.swift`
  - docs/tooling:
    - `README.md`
    - `Scrollapp/README.md`
    - `ScrollappTests/README.md`
    - `scripts/README.md`
    - `scripts/verify_launch_smoke.sh`
    - `scripts/verify_autoscroll_delivery.sh`
    - `scripts/verify_autoscroll_external_no_cursor.sh`
    - archived job records under `docs/jobs/archive/`
  - global local-only skill/policy files:
    - `/Users/brucenguyen/My Drive (trungqua@ualberta.ca)/.codex/skills/execution/simplify/SKILL.md`
    - `/Users/brucenguyen/.codex/notes/simplify_minimum_effective_design_principles.md`
- Key findings and decisions:
  - simplification alone was not enough protection; live runtime feel changed even though verification initially looked acceptable
  - the restored app uses the simpler runtime structure plus the recovered rate-based feel and a higher top-end ceiling
  - future simplification work should treat old-vs-new behavior parity as a blocking gate, not a best effort
  - the `.codex` skill updates are saved locally but are not part of the Scrollapp Git history because `~/.codex` is not a Git repo here
- Open issues or next steps:
  - `docs/TODO.md` remains untracked and was intentionally left out of the commit because it appeared unrelated
