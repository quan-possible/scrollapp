# 2026-03-14

## Conversation Save: autoscroll-delivery-debug
- Context:
  - resumed the ongoing autoscroll simplification/debugging thread from `tmp/autoscroll-simplify-ongoing.md`
  - user reported that behavior had become random across sites, then regressed into "does not work", "cursor gets sucked", and "icon shows but no scrolling"
- User goal:
  - simplify the implementation
  - make autoscroll generalizable across web pages
  - keep scrolling active even if the cursor leaves the original element
  - avoid OS-scroll style testing and rely on code/runtime debugging instead
- Key files inspected or edited during the session:
  - `Scrollapp/ScrollappApp.swift`
  - `Scrollapp/AutoscrollCore.swift`
  - `ScrollappTests/AutoscrollCoreTests.swift`
  - `tmp/autoscroll-simplify-ongoing.md`
- Key execution and verification:
  - `xcodebuild test -project Scrollapp.xcodeproj -scheme Scrollapp -destination 'platform=macOS' -only-testing:ScrollappTests/AutoscrollCoreTests`
    - passed with `19` tests
  - `xcodebuild build -project Scrollapp.xcodeproj -scheme Scrollapp -destination 'platform=macOS'`
    - passed
  - refreshed `/Applications/Scrollapp.app` from the verified build and relaunched it
- Key findings and decisions:
  - a broad URL-ancestor rule was making ordinary web content look linked and was narrowed
  - `postToPid(...)` was not a stable wheel-delivery path here
  - forcing synthetic wheel-event location to the latched anchor point reintroduced cursor-pull behavior
  - the current code was rolled back to the smallest stable delivery path:
    - session-tap posting
    - no forced event-location override
    - live-pointer diagnostics
  - focused tests remained green, but live browser behavior still did not reach acceptable reliability
- Open issues:
  - no clean runtime solution yet for target-latched scrolling after the pointer leaves the clicked element
  - browser automation is not trustworthy validation for the global event-tap path
  - the next implementation pass should add runtime instrumentation before more delivery heuristics
- Files modified in the current repo state:
  - `Scrollapp.xcodeproj/project.pbxproj`
  - `Scrollapp/AutoscrollCore.swift`
  - `Scrollapp/ScrollappApp.swift`
  - `ScrollappTests/AutoscrollCoreTests.swift`
  - `ScrollappUITests/ScrollappUITests.swift`
  - deleted:
    - `Scrollapp/ContentView.swift`
    - `ScrollappTests/ScrollappTests.swift`
    - `ScrollappUITests/ScrollappUITestsLaunchTests.swift`
