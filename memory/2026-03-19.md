# 2026-03-19

## Conversation Save: portfolio-registration
- Context:
  - register Scrollapp as an explicitly tracked project in the Codex personal and manager portfolios
- User goal:
  - add the Scrollapp app repo to the codex-manager project portfolio
- What was inspected or executed:
  - re-read the manager, sub-manager, and project memory contracts before updating the registries
  - verified the live project root and Git remote at `/Users/brucenguyen/Library/CloudStorage/GoogleDrive-bruce.quan.nguyen@gmail.com/My Drive/Projects/scrollapp`
  - updated the personal and manager `projects.json` registries to include `scrollapp`
  - extended the shared runtime env whitelist so `${SCROLLAPP_ROOT}` resolves through the same launcher/bootstrap path as the other tracked projects
- Key files created or updated:
  - `MEMORY.md`
  - `memory/2026-03-19.md`
- Key findings and decisions:
  - `scrollapp` belongs to personal scope, not ACLMR scope
  - the canonical project id is `scrollapp`
  - the canonical repo URL is `https://github.com/quan-possible/scrollapp.git`
- Open issues or next steps:
  - non-mac machine profiles do not yet declare a `SCROLLAPP_ROOT`, so manager-side Scrollapp resolution is only verified on the live macOS profile in this pass
