# Archived Jobs

This folder holds completed job records.

- Files here should not use the `-ongoing.md` suffix.
- Use the filename convention `YYYY-MM-DD-<job-slug>.md`.
- Active in-progress job state belongs in `docs/jobs/`.
- Keep archived job records here when the history is worth preserving.
